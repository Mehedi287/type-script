var numbers = [23, 2, 0, 32, 0, 32, 0];
console.log(numbers);
b;
var max = 0;
for (var _i = 0, numbers_1 = numbers; _i < numbers_1.length; _i++) {
    var number = numbers_1[_i];
    if (number > max) {
        max = number;
    }
}
